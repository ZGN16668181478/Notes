1、if条件判断  
            if :  按照缩进进行代码块控制
2、input函数：用来获取用户输入 ，输入完后回车才会继续向下运行
            返回为一个字符串 可以用来暂时阻止程序结束
3、if-else
4、if-elif-else  最终只实现了一个语句
5、while else循环语句  中else怎么都执行
6、循环语句的嵌套：外层循环执行一次，内层就要执行一圈   即外层控制高度，内层控制宽度
7、break:立即跳出循环 continue:跳出当次循环 pass:占位符，无实际意义