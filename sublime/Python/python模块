模块:命名规范！！
    模块化：讲一个完整的程序分解成一个一个小的模块  通过模块组合，搭建一个完整的模块
    优点：方便开发、方便维护、可以复用
    引入模块：可以在任何地方进行引入，习惯在开头
        import 模块名(就是python名字，不要py)
        import 模块名 as 别名
        from 模块名 import x,y,z
        from xxx import * (xxx里带_的属性无法导入)
        from xxx import x as y
    __name__属性：每个模块都要这个属性，通过这个属性获取到模块的名字。主模块只有一个
    包：是一个文件夹，其中必须要有一个__init__.py文件，包含包中主要内容  
        __pycache__是模块临时缓存，提高运行效率，下次编译直接加载
    sys模块：
        sys.argv:是一个列表，获取执行代码时，命令行中的所有参数
        sys.modules：是一个字典，获取当前模块中引入的所有模块
        sys.path:是一个列表，模块的搜索路径
        sys.platform:当前python的运行环境
        sys.exit([...]):用来退出程序
    pprint模块：pprint()方法，打印数据的简单格式化
    OS模块：对操作系统进行访问
        os.environ：系统的环境变量
        os.system():相当于dos命令操作
